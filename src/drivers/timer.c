#include "timer.h"
#include <stdint.h>
// #define U32_MAX 0xFFFFFFFF
// static uint32_t tick=0;

void timer_handle()
{
    // if(tick==U32_MAX) tick=0;
    // tick++;
    // use for process scheduling?
}